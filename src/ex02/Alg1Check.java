package ex02;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;

import Data_classes.Sample;
import Data_classes.Samples;

public class Alg1Check {
	/*public void check(String path1, String path2){
		ArrayList<String> me = new Samples(path1);
		Samples boaz = new Samples(path2);
		try {
			FileReader fr = new FileReader(path1);
			BufferedReader br = new BufferedReader(fr);
			String str = br.readLine();
			str = br.readLine();
			while(str!=null){
				file.add(new Sample(str.split(",")));
				str = br.readLine();
			}
			br.close();
			fr.close();
		}
		catch(IOException ex) {
			System.out.print("Error reading file\n" + ex);
			System.exit(2);
		}	}
	}*/
}
